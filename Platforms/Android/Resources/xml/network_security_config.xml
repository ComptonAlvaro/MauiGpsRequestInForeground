<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <domain-config>
    <!--NOTA: para que Android confíe en un servidor, se necesitan indicar dos cosas.
        1.- La IP del servidor, o URL.
        Siempre se tiene que indicar, porque en android la seguridad implica también saber a qué servidor se está
        conectando.
        Se pueden incluir tantas IP y dominios como se quiera, en los que se necesite confiar.
    
        2.- El CA con el que se ha emitido el certificado.
        Opcional, solo en caso de utilizar un certificado CA self-signed.
        En caso de que el servidor utilice un certifcado emitido por un CA propio, un self signed, se tendrá
        que idicar el CA en cual confiar y agregarlo en los recursos de la plataforma android.
        Si el certificado del servidor fuera emitido por una entidad certicadora, lo más seguro es que Android lo
        incluya en su sistema por defecto, por lo que no sería necesario incluirlo.
        NOTA: otra opción sería, en cada dispositivo que fuera a utilizar la aplicación, instalar el CA como CA
        de confianza, pero esto haría que el usuario tuviera que hacerlo, de modo que si se incluye aquí, es mucho
        más transparente.
        
        NOTA: es necesario agregar android:networkSecurityConfig="@xml/network_security_config" en el fichero
        android.manifest.xmls, quendando del modo siguiente:
        <application>.... android:networkSecurityConfig="@xml/network_security_config" ...</application>-->
    <domain includeSubdomains="true">gts.globaltecnicosyservicios.cloudns.org</domain>
    <domain includeSubdomains="true">192.168.1.11</domain>
    <domain includeSubdomains="true">192.168.1.132</domain>
    <trust-anchors>
      <certificates src="@raw/ca"/>
    </trust-anchors>
  </domain-config>
</network-security-config>


